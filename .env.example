DISCORD_TOKEN=""
DISCORD_SERVER_ID=""
DISCORD_OWNER_ID=""
DATABASE_URL="sqlite:./db.sqlite"
# IPC_SOCKET_PATH=""  # defaults to $XDG_RUNTIME_DIR/rustvoice.sock (or ~/.local/run, or /tmp)
# DEFAULT_CHANNEL_NAME_TEMPLATE=""  # defaults to "[{game}]"; per-guild override via /config channel-name
# METRICS_ADDR=""  # defaults to 127.0.0.1:9091; only served when built with --features metrics
